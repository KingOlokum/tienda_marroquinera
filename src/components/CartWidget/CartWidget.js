import * as Icon from 'react-bootstrap-icons';

export const CartWidget = () => {
  return (
    <div>
        <Icon.Cart />
        <div>
            <span>2</span>
        </div>
        
    </div>
  )
}
